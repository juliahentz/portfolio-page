describe('contactService', function() {

  beforeEach(module('app'));

  it('should ...', inject(function(contactService) {

    //expect(contactService.doSomething()).toEqual('something');

  }));

});
